[package]
name = "covalence-logic-lrat"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Userspace LRAT proof loading through the checked HOL kernel"
publish = false

[features]
default = ["parse"]
parse = []

[dependencies]
covalence-lib-error = { path = "../../lib/error" }
covalence-logic-hol = { path = "../hol" }
covalence-logic-sat = { path = "../sat" }

[lints]
workspace = true
