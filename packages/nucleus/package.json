{
  "name": "@nucleus/nucleus",
  "version": "0.0.0",
  "private": true,
  "license": "CC0-1.0",
  "type": "module",
  "files": [
    "dist",
    "generated",
    "demo.html"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./wasm": "./generated/nucleus_bg.wasm",
    "./shell-wasm": "./generated/shell.wasm"
  },
  "scripts": {
    "generate": "cargo build --locked -p covalence-browser --target wasm32-unknown-unknown && wasm-bindgen ../../target/wasm32-unknown-unknown/debug/covalence_browser.wasm --out-dir generated --out-name nucleus --target web && cargo build --locked --profile wasm-release -p covalence-bin-cas-shell --target wasm32-wasip1 && cp ../../target/wasm32-wasip1/wasm-release/covalence-cas-shell.wasm generated/shell.wasm",
    "build": "pnpm run generate && tsc",
    "check": "tsc --noEmit",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "playwright-core": "1.61.1",
    "typescript": "5.9.2"
  }
}
