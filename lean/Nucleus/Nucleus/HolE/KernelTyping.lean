import Nucleus.HolE.ClassicalSoundness

/-! # Typing inversion for HolE proof certificates -/

/-! `Proves.conclusionTyping` is now the generated common-field projection of
the kernel certificate family.  This compatibility module retains the focused
import boundary used by downstream developments. -/
